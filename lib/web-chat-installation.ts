/**
 * Default installation for the in-browser channel. Seeded in
 * {@link "@/core/installations/repo"} as `demo-web` / `connectorKind: "web"`.
 */
export const WEB_CHAT_INSTALLATION_ID = "demo-web";
